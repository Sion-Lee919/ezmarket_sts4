<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="google.GithubUserMapper">
    <select id="countByGithubId" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM member WHERE username = #{githubId}
    </select>

    <insert id="saveUser" >
    INSERT INTO member (member_id, username, realname, password, email, nickname, phone)
    VALUES (TRUNC(DBMS_RANDOM.VALUE(100000000000000, 999999999999999)), #{githubId}, #{name}, TRUNC(DBMS_RANDOM.VALUE(100000000000000, 999999999999999)), #{email}, '닉네임을 변경해주세요', '010-0000-0000')
</insert>

    <update id="updateUser">
        UPDATE users_github
        SET email = #{email}, realname = #{name}
        WHERE username = #{githubId}
    </update>
</mapper>