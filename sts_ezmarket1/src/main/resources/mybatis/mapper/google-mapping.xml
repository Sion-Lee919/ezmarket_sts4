<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="google.UserMapper">
    <select id="countByGoogleId" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM users_google WHERE google_id = #{googleId}
    </select>

    <insert id="saveUser" >
    INSERT INTO users_google (google_id, email, name, picture)
    VALUES (#{googleId}, #{email}, #{name}, #{picture})
</insert>

    <update id="updateUser">
        UPDATE users_google 
        SET email = #{email}, name = #{name}, picture = #{picture}
        WHERE google_id = #{googleId}
    </update>
</mapper>